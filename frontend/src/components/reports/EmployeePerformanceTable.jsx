import { Card, CardContent, Skeleton, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Typography } from '@mui/material'
const columns = ['Employee', 'Assigned Leads', 'Calls Made', 'Completed Calls', 'Pending Follow-ups', 'Average Lead Score']
const EmployeePerformanceTable = ({ data = [], loading }) => <Card sx={{ borderRadius: 4, boxShadow: '0 8px 25px rgba(0,0,0,.08)' }}><CardContent sx={{ p: 2.5 }}><Typography variant="h6" fontWeight={700} sx={{ mb: 2 }}>Employee Performance</Typography>{loading ? <Skeleton variant="rounded" height={240} /> : <TableContainer><Table size="small"><TableHead><TableRow>{columns.map((column) => <TableCell key={column} sx={{ fontWeight: 700, whiteSpace: 'nowrap' }}>{column}</TableCell>)}</TableRow></TableHead><TableBody>{data.map((item) => <TableRow key={item.employeeId} hover><TableCell>{item.employee}</TableCell><TableCell>{item.assignedLeads}</TableCell><TableCell>{item.callsMade}</TableCell><TableCell>{item.completedCalls}</TableCell><TableCell>{item.pendingFollowups}</TableCell><TableCell>{item.averageLeadScore}</TableCell></TableRow>)}</TableBody></Table></TableContainer>}</CardContent></Card>
export default EmployeePerformanceTable
