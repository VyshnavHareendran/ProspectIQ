import FileDownloadRoundedIcon from '@mui/icons-material/FileDownloadRounded'
import { Button, Card, CardContent, Grid, MenuItem, TextField } from '@mui/material'
const ranges = [{ value: 'today', label: 'Today' }, { value: 'last7', label: 'Last 7 Days' }, { value: 'last30', label: 'Last 30 Days' }, { value: 'custom', label: 'Custom' }]
const cities = ['Kochi', 'Trivandrum', 'Chennai', 'Bangalore']
const ReportFilters = ({ employees = [], filters, onChange, onExport }) => <Card sx={{ borderRadius: 5, boxShadow: '0 8px 25px rgba(0,0,0,.08)' }}><CardContent><Grid container spacing={2} alignItems="center"><Grid size={{ xs: 12, sm: 6, md: 3 }}><TextField select fullWidth size="small" label="Date Range" value={filters.dateRange} onChange={(event) => onChange('dateRange', event.target.value)}>{ranges.map((range) => <MenuItem key={range.value} value={range.value}>{range.label}</MenuItem>)}</TextField></Grid><Grid size={{ xs: 12, sm: 6, md: 3 }}><TextField select fullWidth size="small" label="Employee" value={filters.employeeId} onChange={(event) => onChange('employeeId', event.target.value)}><MenuItem value="">All Employees</MenuItem>{employees.map((employee) => <MenuItem key={employee.id} value={employee.id}>{employee.name}</MenuItem>)}</TextField></Grid><Grid size={{ xs: 12, sm: 6, md: 3 }}><TextField select fullWidth size="small" label="City" value={filters.city} onChange={(event) => onChange('city', event.target.value)}><MenuItem value="">All Cities</MenuItem>{cities.map((city) => <MenuItem key={city} value={city}>{city}</MenuItem>)}</TextField></Grid><Grid size={{ xs: 12, sm: 6, md: 3 }}><Button fullWidth variant="contained" startIcon={<FileDownloadRoundedIcon />} onClick={onExport} sx={{ height: 40, textTransform: 'none', fontWeight: 700 }}>Export Report</Button></Grid></Grid></CardContent></Card>
export default ReportFilters
