import FiberManualRecordRoundedIcon from '@mui/icons-material/FiberManualRecordRounded'
import { Box, Card, CardContent, Skeleton, Stack, Typography } from '@mui/material'
const RecentActivityTimeline = ({ activities = [], loading }) => <Card sx={{ borderRadius: 4, boxShadow: '0 8px 25px rgba(0,0,0,.08)' }}><CardContent sx={{ p: 2.5 }}><Typography variant="h6" fontWeight={700} sx={{ mb: 2 }}>Recent Activities</Typography>{loading ? <Skeleton variant="rounded" height={220} /> : <Stack spacing={0}>{activities.map((activity, index) => <Stack key={activity.id} direction="row" spacing={1.5} sx={{ minHeight: 72 }}><Stack alignItems="center"><FiberManualRecordRoundedIcon color="primary" sx={{ fontSize: 16, mt: 0.4 }} />{index < activities.length - 1 && <Box sx={{ width: 2, flexGrow: 1, bgcolor: 'divider', my: 0.5 }} />}</Stack><Box sx={{ pb: 2 }}><Typography fontWeight={700} variant="body2">{activity.title}</Typography><Typography color="text.secondary" variant="caption" display="block">{activity.detail}</Typography><Typography color="text.secondary" variant="caption">{activity.timestamp}</Typography></Box></Stack>)}</Stack>}</CardContent></Card>
export default RecentActivityTimeline
